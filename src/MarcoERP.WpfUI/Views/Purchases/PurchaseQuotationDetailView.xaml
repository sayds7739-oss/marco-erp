<UserControl x:Class="MarcoERP.WpfUI.Views.Purchases.PurchaseQuotationDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:common="clr-namespace:MarcoERP.WpfUI.Common"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Row 0: Header — Back + Title + Status + Actions -->
        <Border Grid.Row="0" Style="{StaticResource CardPanel}" Padding="12,8" Margin="0,0,0,4">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                            Command="{Binding BackCommand}" ToolTip="العودة للقائمة"
                            Padding="4" Height="32" Width="32" Margin="0,0,8,0">
                        <materialDesign:PackIcon Kind="ArrowRight" Width="20" Height="20" />
                    </Button>
                    <TextBlock Text="طلب شراء" Style="{StaticResource PageTitle}"
                               VerticalAlignment="Center" Margin="0" />
                    <Border CornerRadius="12" Padding="12,4" Margin="12,0,0,0"
                            Visibility="{Binding CurrentQuotation, Converter={StaticResource NullToVisibilityConverter}}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{StaticResource WarningBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAccepted}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource SuccessBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsRejected}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource DangerBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsConverted}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource InfoBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Foreground="White" FontSize="12" FontWeight="SemiBold"
                                   Text="{Binding CurrentQuotation.Status, Converter={StaticResource StatusToArabicConverter}}" />
                    </Border>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" HorizontalAlignment="Left">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding EditCommand}" Margin="4,0"
                            Visibility="{Binding CanEdit, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Pencil" VerticalAlignment="Center" />
                            <TextBlock Text="تعديل" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource SuccessBrush}" Foreground="White"
                            Command="{Binding SaveCommand}" Margin="4,0"
                            Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" VerticalAlignment="Center" />
                            <TextBlock Text="حفظ" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding CancelEditCommand}" Margin="4,0"
                            Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cancel" VerticalAlignment="Center" />
                            <TextBlock Text="إلغاء التعديل" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource InfoBrush}" Foreground="White"
                            Command="{Binding SendCommand}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Send" VerticalAlignment="Center" />
                            <TextBlock Text="إرسال" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource SuccessBrush}" Foreground="White"
                            Command="{Binding AcceptCommand}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CheckCircle" VerticalAlignment="Center" />
                            <TextBlock Text="قبول" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource WarningBrush}" Foreground="White"
                            Command="{Binding RejectCommand}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CloseCircle" VerticalAlignment="Center" />
                            <TextBlock Text="رفض" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#1565C0" Foreground="White"
                            Command="{Binding ConvertToInvoiceCommand}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileDocumentArrowRight" VerticalAlignment="Center" />
                            <TextBlock Text="تحويل لفاتورة" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource DangerBrush}" Foreground="White"
                            Command="{Binding CancelQuotationCommand}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cancel" VerticalAlignment="Center" />
                            <TextBlock Text="إلغاء" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource DangerBrush}" Foreground="White"
                            Command="{Binding DeleteDraftCommand}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Delete" VerticalAlignment="Center" />
                            <TextBlock Text="حذف" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,4" />

                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Command="{Binding GoToPreviousCommand}" ToolTip="الطلب السابق"
                            Padding="4" Margin="4,0,0,0" Foreground="{StaticResource PrimaryBrush}">
                        <materialDesign:PackIcon Kind="ChevronRight" Width="20" Height="20" />
                    </Button>
                    <TextBlock Text="{Binding NavigationPositionText}"
                               VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource SubtitleBrush}" Margin="4,0" />

                    <TextBox Width="140" Margin="6,0"
                             materialDesign:HintAssist.Hint="رقم طلب الشراء"
                             materialDesign:HintAssist.IsFloating="True"
                             Text="{Binding JumpQuotationNumber, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             common:TextBoxEnterCommandBehavior.Command="{Binding JumpToQuotationCommand}" />
                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Command="{Binding JumpToQuotationCommand}" ToolTip="انتقال" Padding="4"
                            Foreground="{StaticResource PrimaryBrush}">
                        <materialDesign:PackIcon Kind="ArrowRightBold" Width="18" Height="18" />
                    </Button>
                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Command="{Binding GoToNextCommand}" ToolTip="الطلب التالي"
                            Padding="4" Margin="0,0,4,0" Foreground="{StaticResource PrimaryBrush}">
                        <materialDesign:PackIcon Kind="ChevronLeft" Width="20" Height="20" />
                    </Button>

                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding PrintCommand}" Margin="4,0" Height="28" Padding="8,0"
                            ToolTip="طباعة عرض السعر">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Printer" Width="14" Height="14" VerticalAlignment="Center" />
                            <TextBlock Text="طباعة" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="11" />
                        </StackPanel>
                    </Button>

                    <Border CornerRadius="8" Padding="8,2" Margin="8,0,0,0"
                            Background="{StaticResource UnsavedWarningBackgroundBrush}"
                            Visibility="{Binding IsDirty, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CircleEditOutline" Width="14" Height="14"
                                                     Foreground="{StaticResource UnsavedWarningBrush}" VerticalAlignment="Center" />
                            <TextBlock Text="غير محفوظ" FontSize="11" Foreground="{StaticResource UnsavedWarningBrush}"
                                       Margin="4,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Row 1: Error Bar -->
        <Border Grid.Row="1" Background="{StaticResource ErrorBackgroundBrush}" CornerRadius="4" Padding="10,6" Margin="8,2"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AlertCircle" Foreground="{StaticResource ErrorForegroundBrush}" VerticalAlignment="Center" />
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorForegroundBrush}"
                           Margin="8,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Row 2: Form Body -->
        <Border Grid.Row="2" Style="{StaticResource CardPanel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="140" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="Auto" MinWidth="130" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="Auto" MinWidth="130" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="رقم طلب الشراء"
                                 materialDesign:HintAssist.IsFloating="True"
                                 Text="{Binding FormNumber}" IsReadOnly="True"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}" />
                        <DatePicker Grid.Column="2" materialDesign:HintAssist.Hint="التاريخ"
                                    materialDesign:HintAssist.IsFloating="True"
                                    SelectedDate="{Binding FormDate}" IsEnabled="{Binding IsEditing}"
                                    Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                    Width="130" />
                        <DatePicker Grid.Column="4" materialDesign:HintAssist.Hint="صالح حتى"
                                    materialDesign:HintAssist.IsFloating="True"
                                    SelectedDate="{Binding FormValidUntil}" IsEnabled="{Binding IsEditing}"
                                    Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                    Width="130" />
                    </Grid>

                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" materialDesign:HintAssist.Hint="المورد (F1 للبحث)"
                                  materialDesign:HintAssist.IsFloating="True"
                                  ItemsSource="{Binding Suppliers}" SelectedValue="{Binding FormSupplierId}"
                                  SelectedValuePath="Id" DisplayMemberPath="NameAr"
                                  IsEnabled="{Binding IsEditing}" IsEditable="True"
                                  common:F1SearchBehavior.IsEnabled="True"
                                  common:F1SearchBehavior.Title="بحث الموردين"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                        <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="المخزن (F1 للبحث)"
                                  materialDesign:HintAssist.IsFloating="True"
                                  ItemsSource="{Binding Warehouses}" SelectedValue="{Binding FormWarehouseId}"
                                  SelectedValuePath="Id" DisplayMemberPath="NameAr"
                                  IsEnabled="{Binding IsEditing}" IsEditable="True"
                                  common:F1SearchBehavior.IsEnabled="True"
                                  common:F1SearchBehavior.Title="بحث المخازن"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                    </Grid>

                    <DockPanel Margin="0,12,0,0">
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="بنود طلب الشراء" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" />
                            <Button Style="{StaticResource MaterialDesignFlatButton}"
                                    Command="{Binding AddLineCommand}" IsEnabled="{Binding IsEditing}"
                                    Margin="8,0,0,0" Padding="4" Height="28" Width="28" ToolTip="إضافة بند">
                                <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" />
                            </Button>
                        </StackPanel>
                        <DataGrid ItemsSource="{Binding FormLines}" AutoGenerateColumns="False"
                                  CanUserAddRows="False" CanUserDeleteRows="False"
                                  SelectionMode="Single" GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column" FontSize="12"
                                  RowHeight="36" ColumnHeaderHeight="34" BorderThickness="1"
                                  BorderBrush="#E0E0E0" MinHeight="180"
                                  materialDesign:DataGridAssist.CellPadding="6 4"
                                  materialDesign:DataGridAssist.ColumnHeaderPadding="6 6">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="الصنف" Width="*" MinWidth="160">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding DataContext.Products, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      SelectedValue="{Binding ProductId, UpdateSourceTrigger=PropertyChanged}"
                                                      SelectedValuePath="Id" DisplayMemberPath="NameAr"
                                                      IsEnabled="{Binding DataContext.IsEditing, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      IsEditable="True" FontSize="12" BorderThickness="0"
                                                      common:F1SearchBehavior.IsEnabled="True"
                                                      common:F1SearchBehavior.Title="بحث الأصناف" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="الوحدة" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding AvailableUnits}"
                                                      SelectedValue="{Binding UnitId, UpdateSourceTrigger=PropertyChanged}"
                                                      SelectedValuePath="UnitId" DisplayMemberPath="UnitNameAr"
                                                      IsEnabled="{Binding DataContext.IsEditing, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      FontSize="12" BorderThickness="0" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="الكمية" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged, StringFormat=0.00}"
                                                     IsEnabled="{Binding DataContext.IsEditing, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                     TextAlignment="Center" BorderThickness="0" FontSize="12" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="السعر" Width="90">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"
                                                     IsEnabled="{Binding DataContext.IsEditing, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                     TextAlignment="Center" BorderThickness="0" FontSize="12" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="خصم%" Width="65">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding DiscountPercent, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}"
                                                     IsEnabled="{Binding DataContext.IsEditing, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                     TextAlignment="Center" BorderThickness="0" FontSize="12" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="الإجمالي" Width="95" Binding="{Binding SubTotal, StringFormat=N2}" IsReadOnly="True" />
                                <DataGridTextColumn Header="الضريبة" Width="85" Binding="{Binding VatAmount, StringFormat=N2}" IsReadOnly="True" />
                                <DataGridTextColumn Header="الصافي" Width="95" Binding="{Binding TotalWithVat, StringFormat=N2}" IsReadOnly="True" />
                                <DataGridTemplateColumn Header="" Width="36">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Style="{StaticResource MaterialDesignFlatButton}"
                                                    Command="{Binding DataContext.RemoveLineCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    IsEnabled="{Binding DataContext.IsEditing, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    Padding="2" Height="24" Width="24" Foreground="{StaticResource DangerBrush}" ToolTip="حذف البند">
                                                <materialDesign:PackIcon Kind="Delete" Width="14" Height="14" />
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>

                    <TextBox materialDesign:HintAssist.Hint="ملاحظات" materialDesign:HintAssist.IsFloating="True"
                             Text="{Binding FormNotes, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsEditing}" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,8,0,0" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="50" MaxHeight="80" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Row 3: Totals Bar -->
        <Border Grid.Row="3" Background="{StaticResource BackgroundBrush}" CornerRadius="4" Padding="16,10" Margin="8,4,8,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="الإجمالي" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding TotalSubtotal, StringFormat=N2}" FontWeight="SemiBold" FontSize="16" HorizontalAlignment="Center" />
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="الخصم" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding TotalDiscount, StringFormat=N2}" FontWeight="SemiBold" FontSize="16" Foreground="{StaticResource WarningBrush}" HorizontalAlignment="Center" />
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="الضريبة" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding TotalVat, StringFormat=N2}" FontWeight="SemiBold" FontSize="16" Foreground="{StaticResource DangerBrush}" HorizontalAlignment="Center" />
                </StackPanel>
                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                    <TextBlock Text="الصافي" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding TotalNet, StringFormat=N2}" FontWeight="Bold" FontSize="18" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Row 4: Status Bar -->
        <Border Grid.Row="4" Background="#ECEFF1" CornerRadius="4" Padding="10,6" Margin="8,4,8,0">
            <DockPanel>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource StatusTextBrush}" VerticalAlignment="Center" DockPanel.Dock="Right" />
                <ProgressBar IsIndeterminate="{Binding IsBusy}" Height="3"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                             DockPanel.Dock="Left" Width="120" Margin="0,0,10,0" VerticalAlignment="Center" />
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
