<UserControl x:Class="MarcoERP.WpfUI.Views.Reports.IncomeStatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">
    <DockPanel Margin="16">
        <Border DockPanel.Dock="Top" Style="{StaticResource CardPanel}" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="قائمة الدخل" Style="{StaticResource PageTitle}" Margin="0,0,0,12" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="من:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding FromDate}" Width="140" Margin="0,0,16,0" />
                    <TextBlock Text="إلى:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding ToDate}" Width="140" Margin="0,0,16,0" />
                    <Button Content="عرض" Command="{Binding GenerateCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="16,6" />
                    <Button Content="معاينة PDF" Command="{Binding ExportPdfCommand}" Margin="8,0,0,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="12,4" />
                    <Button Content="Excel" Command="{Binding ExportExcelCommand}" Margin="4,0,0,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="12,4" />
                </StackPanel>
            </StackPanel>
        </Border>
        <Border DockPanel.Dock="Bottom" Background="{StaticResource ErrorBackgroundBrush}" CornerRadius="4" Padding="8" Margin="0,4,0,0"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" FontSize="12" />
        </Border>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Revenue Section -->
                <Border Style="{StaticResource CardPanel}" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="الإيرادات" Style="{StaticResource SectionTitle}" Foreground="{StaticResource SuccessBrush}" Margin="0,0,0,8" />
                        <DataGrid ItemsSource="{Binding RevenueRows}" AutoGenerateColumns="False" IsReadOnly="True"
                                  CanUserAddRows="False" HeadersVisibility="None" Background="Transparent"
                                  materialDesign:DataGridAssist.CellPadding="8 4" GridLinesVisibility="None">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding AccountCode}" Width="100" />
                                <DataGridTextColumn Binding="{Binding AccountNameAr}" Width="*" />
                                <DataGridTextColumn Binding="{Binding Amount, StringFormat=N2}" Width="150">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Left" /></Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Separator Margin="0,4" />
                        <DockPanel Margin="8,4">
                            <TextBlock Text="إجمالي الإيرادات" FontWeight="Bold" />
                            <TextBlock Text="{Binding TotalRevenue, StringFormat=N2}" FontWeight="Bold"
                                       Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Left" Margin="50,0,0,0" />
                        </DockPanel>
                    </StackPanel>
                </Border>
                <!-- COGS Section -->
                <Border Style="{StaticResource CardPanel}" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="تكلفة البضاعة المباعة" Style="{StaticResource SectionTitle}" Foreground="{StaticResource WarningBrush}" Margin="0,0,0,8" />
                        <DataGrid ItemsSource="{Binding CogsRows}" AutoGenerateColumns="False" IsReadOnly="True"
                                  CanUserAddRows="False" HeadersVisibility="None" Background="Transparent"
                                  materialDesign:DataGridAssist.CellPadding="8 4" GridLinesVisibility="None">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding AccountCode}" Width="100" />
                                <DataGridTextColumn Binding="{Binding AccountNameAr}" Width="*" />
                                <DataGridTextColumn Binding="{Binding Amount, StringFormat=N2}" Width="150" />
                            </DataGrid.Columns>
                        </DataGrid>
                        <Separator Margin="0,4" />
                        <DockPanel Margin="8,4">
                            <TextBlock Text="تكلفة المبيعات" FontWeight="Bold" />
                            <TextBlock Text="{Binding TotalCogs, StringFormat=N2}" FontWeight="Bold"
                                       Foreground="{StaticResource WarningBrush}" HorizontalAlignment="Left" Margin="50,0,0,0" />
                        </DockPanel>
                    </StackPanel>
                </Border>
                <!-- Gross Profit -->
                <Border Background="{StaticResource PrimaryBrush}" CornerRadius="4" Padding="16,10" Margin="0,0,0,8">
                    <DockPanel>
                        <TextBlock Text="مجمل الربح" FontWeight="Bold" FontSize="16" Foreground="White" />
                        <TextBlock Text="{Binding GrossProfit, StringFormat=N2}" FontWeight="Bold" FontSize="16"
                                   Foreground="White" HorizontalAlignment="Left" Margin="50,0,0,0" />
                    </DockPanel>
                </Border>
                <!-- Expenses Section -->
                <Border Style="{StaticResource CardPanel}" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="المصروفات التشغيلية" Style="{StaticResource SectionTitle}" Foreground="{StaticResource DangerBrush}" Margin="0,0,0,8" />
                        <DataGrid ItemsSource="{Binding ExpenseRows}" AutoGenerateColumns="False" IsReadOnly="True"
                                  CanUserAddRows="False" HeadersVisibility="None" Background="Transparent"
                                  materialDesign:DataGridAssist.CellPadding="8 4" GridLinesVisibility="None">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding AccountCode}" Width="100" />
                                <DataGridTextColumn Binding="{Binding AccountNameAr}" Width="*" />
                                <DataGridTextColumn Binding="{Binding Amount, StringFormat=N2}" Width="150" />
                            </DataGrid.Columns>
                        </DataGrid>
                        <Separator Margin="0,4" />
                        <DockPanel Margin="8,4">
                            <TextBlock Text="إجمالي المصروفات" FontWeight="Bold" />
                            <TextBlock Text="{Binding TotalExpenses, StringFormat=N2}" FontWeight="Bold"
                                       Foreground="{StaticResource DangerBrush}" HorizontalAlignment="Left" Margin="50,0,0,0" />
                        </DockPanel>
                    </StackPanel>
                </Border>
                <!-- Other Income -->
                <Border Style="{StaticResource CardPanel}" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="إيرادات أخرى" Style="{StaticResource SectionTitle}" Margin="0,0,0,8" />
                        <DataGrid ItemsSource="{Binding OtherIncomeRows}" AutoGenerateColumns="False" IsReadOnly="True"
                                  CanUserAddRows="False" HeadersVisibility="None" Background="Transparent"
                                  materialDesign:DataGridAssist.CellPadding="8 4" GridLinesVisibility="None">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding AccountCode}" Width="100" />
                                <DataGridTextColumn Binding="{Binding AccountNameAr}" Width="*" />
                                <DataGridTextColumn Binding="{Binding Amount, StringFormat=N2}" Width="150" />
                            </DataGrid.Columns>
                        </DataGrid>
                        <Separator Margin="0,4" />
                        <DockPanel Margin="8,4">
                            <TextBlock Text="إجمالي إيرادات أخرى" FontWeight="Bold" />
                            <TextBlock Text="{Binding TotalOtherIncome, StringFormat=N2}" FontWeight="Bold" HorizontalAlignment="Left" Margin="50,0,0,0" />
                        </DockPanel>
                    </StackPanel>
                </Border>
                <!-- Other Expenses -->
                <Border Style="{StaticResource CardPanel}" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="مصروفات أخرى" Style="{StaticResource SectionTitle}" Margin="0,0,0,8" />
                        <DataGrid ItemsSource="{Binding OtherExpenseRows}" AutoGenerateColumns="False" IsReadOnly="True"
                                  CanUserAddRows="False" HeadersVisibility="None" Background="Transparent"
                                  materialDesign:DataGridAssist.CellPadding="8 4" GridLinesVisibility="None">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding AccountCode}" Width="100" />
                                <DataGridTextColumn Binding="{Binding AccountNameAr}" Width="*" />
                                <DataGridTextColumn Binding="{Binding Amount, StringFormat=N2}" Width="150" />
                            </DataGrid.Columns>
                        </DataGrid>
                        <Separator Margin="0,4" />
                        <DockPanel Margin="8,4">
                            <TextBlock Text="إجمالي مصروفات أخرى" FontWeight="Bold" />
                            <TextBlock Text="{Binding TotalOtherExpenses, StringFormat=N2}" FontWeight="Bold" HorizontalAlignment="Left" Margin="50,0,0,0" />
                        </DockPanel>
                    </StackPanel>
                </Border>
                <!-- Net Profit -->
                <Border CornerRadius="4" Padding="16,12" Margin="0,0,0,8">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="{StaticResource SuccessBrush}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding NetProfit, Converter={StaticResource IsNegativeConverter}}" Value="True">
                                    <Setter Property="Background" Value="{StaticResource DangerBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <DockPanel>
                        <TextBlock FontWeight="Bold" FontSize="18" Foreground="White">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="صافي الربح" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding NetProfit, Converter={StaticResource IsNegativeConverter}}" Value="True">
                                            <Setter Property="Text" Value="صافي الخسارة" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding NetProfit, StringFormat=N2}" FontWeight="Bold" FontSize="18"
                                   Foreground="White" HorizontalAlignment="Left" Margin="50,0,0,0" />
                    </DockPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
