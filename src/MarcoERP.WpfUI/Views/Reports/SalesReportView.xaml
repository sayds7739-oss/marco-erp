<UserControl x:Class="MarcoERP.WpfUI.Views.Reports.SalesReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:common="clr-namespace:MarcoERP.WpfUI.Common"
             FlowDirection="RightToLeft">
    <DockPanel Margin="16">
        <Border DockPanel.Dock="Top" Style="{StaticResource CardPanel}" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="تقرير المبيعات" Style="{StaticResource PageTitle}" Margin="0,0,0,12" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="العميل:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ComboBox ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}"
                              DisplayMemberPath="NameAr" Width="200" IsEditable="True"
                              materialDesign:HintAssist.Hint="الكل (F1 للبحث)"
                              common:F1SearchBehavior.IsEnabled="True"
                              common:F1SearchBehavior.Title="بحث العملاء"
                              Margin="0,0,16,0" />
                    <TextBlock Text="من:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding FromDate}" Width="140" Margin="0,0,16,0" />
                    <TextBlock Text="إلى:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding ToDate}" Width="140" Margin="0,0,16,0" />
                    <Button Content="عرض" Command="{Binding GenerateCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="16,6" />
                    <Button Content="معاينة PDF" Command="{Binding ExportPdfCommand}" Margin="8,0,0,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="12,4" />
                    <Button Content="Excel" Command="{Binding ExportExcelCommand}" Margin="4,0,0,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="12,4" />
                </StackPanel>
            </StackPanel>
        </Border>
        <Border DockPanel.Dock="Bottom" Style="{StaticResource CardPanel}" Padding="12" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="عدد الفواتير:" FontWeight="SemiBold" Margin="0,0,4,0" />
                <TextBlock Text="{Binding InvoiceCount}" FontWeight="Bold" Margin="0,0,24,0" />
                <TextBlock Text="إجمالي الخصم:" FontWeight="SemiBold" Margin="0,0,4,0" />
                <TextBlock Text="{Binding TotalDiscount, StringFormat=N2}" Margin="0,0,24,0" />
                <TextBlock Text="إجمالي الضريبة:" FontWeight="SemiBold" Margin="0,0,4,0" />
                <TextBlock Text="{Binding TotalVat, StringFormat=N2}" Margin="0,0,24,0" />
                <TextBlock Text="صافي المبيعات:" FontWeight="SemiBold" Margin="0,0,4,0" />
                <TextBlock Text="{Binding TotalNet, StringFormat=N2}" FontWeight="Bold"
                           Foreground="{StaticResource SuccessBrush}" />
            </StackPanel>
        </Border>
        <Border DockPanel.Dock="Bottom" Background="{StaticResource ErrorBackgroundBrush}" CornerRadius="4" Padding="8" Margin="0,4,0,0"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" FontSize="12" />
        </Border>
        <DataGrid ItemsSource="{Binding Rows}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  materialDesign:DataGridAssist.CellPadding="10 6" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceNumber}" Width="120" />
                <DataGridTextColumn Header="التاريخ" Binding="{Binding InvoiceDate, StringFormat=yyyy-MM-dd}" Width="110" />
                <DataGridTextColumn Header="العميل" Binding="{Binding CustomerName}" Width="*" />
                <DataGridTextColumn Header="المبلغ" Binding="{Binding Subtotal, StringFormat=N2}" Width="120" />
                <DataGridTextColumn Header="الخصم" Binding="{Binding DiscountTotal, StringFormat=N2}" Width="100" />
                <DataGridTextColumn Header="الضريبة" Binding="{Binding VatTotal, StringFormat=N2}" Width="100" />
                <DataGridTextColumn Header="الصافي" Binding="{Binding NetTotal, StringFormat=N2}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
