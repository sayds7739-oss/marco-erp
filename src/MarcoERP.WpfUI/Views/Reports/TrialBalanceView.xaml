<UserControl x:Class="MarcoERP.WpfUI.Views.Reports.TrialBalanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">

    <DockPanel Margin="16">

        <!-- Header & Filters -->
        <Border DockPanel.Dock="Top" Style="{StaticResource CardPanel}" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="ميزان المراجعة" Style="{StaticResource PageTitle}" Margin="0,0,0,12" />

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="من:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding FromDate}" Width="150" Margin="0,0,16,0" />
                    <TextBlock Text="إلى:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding ToDate}" Width="150" Margin="0,0,16,0" />
                    <Button Content="عرض التقرير" Command="{Binding GenerateCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="16,6" />
                    <Button Content="معاينة PDF" Command="{Binding ExportPdfCommand}" Margin="8,0,0,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="12,4" />
                    <Button Content="Excel" Command="{Binding ExportExcelCommand}" Margin="4,0,0,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="12,4" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Summary -->
        <Border DockPanel.Dock="Bottom" Style="{StaticResource CardPanel}" Padding="12" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="إجمالي المدين:" FontWeight="SemiBold" Margin="0,0,8,0" />
                <TextBlock Text="{Binding TotalDebit, StringFormat=N2}" FontWeight="Bold"
                           Foreground="{StaticResource SuccessBrush}" Margin="0,0,32,0" />
                <TextBlock Text="إجمالي الدائن:" FontWeight="SemiBold" Margin="0,0,8,0" />
                <TextBlock Text="{Binding TotalCredit, StringFormat=N2}" FontWeight="Bold"
                           Foreground="{StaticResource DangerBrush}" />
            </StackPanel>
        </Border>

        <!-- Error -->
        <Border DockPanel.Dock="Bottom" Background="{StaticResource ErrorBackgroundBrush}" CornerRadius="4" Padding="8" Margin="0,4,0,0"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" FontSize="12" />
        </Border>

        <!-- DataGrid -->
        <DataGrid ItemsSource="{Binding Rows}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False"
                  materialDesign:DataGridAssist.CellPadding="10 6"
                  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                  GridLinesVisibility="Horizontal" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="كود الحساب" Binding="{Binding AccountCode}" Width="100" />
                <DataGridTextColumn Header="اسم الحساب" Binding="{Binding AccountNameAr}" Width="*" />
                <DataGridTextColumn Header="النوع" Binding="{Binding AccountTypeName}" Width="120" />
                <DataGridTextColumn Header="مستوى" Binding="{Binding Level}" Width="60" />
                <DataGridTextColumn Header="مدين" Binding="{Binding TotalDebit, StringFormat=N2}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Left" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="دائن" Binding="{Binding TotalCredit, StringFormat=N2}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Left" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="الرصيد" Binding="{Binding Balance, StringFormat=N2}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Left" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="الجانب" Binding="{Binding BalanceSide}" Width="70" />
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</UserControl>
