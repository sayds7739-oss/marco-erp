<UserControl x:Class="MarcoERP.WpfUI.Views.Reports.VatReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">
    <DockPanel Margin="16">
        <Border DockPanel.Dock="Top" Style="{StaticResource CardPanel}" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="تقرير ضريبة القيمة المضافة" Style="{StaticResource PageTitle}" Margin="0,0,0,12" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="من:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding FromDate}" Width="140" Margin="0,0,16,0" />
                    <TextBlock Text="إلى:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding ToDate}" Width="140" Margin="0,0,16,0" />
                    <Button Content="عرض" Command="{Binding GenerateCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="16,6" />
                    <Button Content="معاينة PDF" Command="{Binding ExportPdfCommand}" Margin="8,0,0,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="12,4" />
                    <Button Content="Excel" Command="{Binding ExportExcelCommand}" Margin="4,0,0,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            materialDesign:ButtonAssist.CornerRadius="4" Padding="12,4" />
                </StackPanel>
            </StackPanel>
        </Border>
        <Border DockPanel.Dock="Bottom" Style="{StaticResource CardPanel}" Padding="16" Margin="0,8,0,0">
            <UniformGrid Columns="3">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="إجمالي ضريبة المبيعات" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding TotalSalesVat, StringFormat=N2}" FontWeight="Bold" FontSize="18"
                               Foreground="{StaticResource DangerBrush}" HorizontalAlignment="Center" />
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="إجمالي ضريبة المشتريات" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding TotalPurchaseVat, StringFormat=N2}" FontWeight="Bold" FontSize="18"
                               Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center" />
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="صافي الضريبة المستحقة" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding NetVatPayable, StringFormat=N2}" FontWeight="Bold" FontSize="20"
                               Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </UniformGrid>
        </Border>
        <Border DockPanel.Dock="Bottom" Background="{StaticResource ErrorBackgroundBrush}" CornerRadius="4" Padding="8" Margin="0,4,0,0"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" FontSize="12" />
        </Border>
        <DataGrid ItemsSource="{Binding Rows}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  materialDesign:DataGridAssist.CellPadding="10 6" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="نسبة الضريبة %" Binding="{Binding VatRate, StringFormat=N1}" Width="110" />
                <DataGridTextColumn Header="أساس المبيعات" Binding="{Binding SalesBaseAmount, StringFormat=N2}" Width="*" />
                <DataGridTextColumn Header="ضريبة المبيعات" Binding="{Binding SalesVatAmount, StringFormat=N2}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource DangerBrush}" /></Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="أساس المشتريات" Binding="{Binding PurchaseBaseAmount, StringFormat=N2}" Width="*" />
                <DataGridTextColumn Header="ضريبة المشتريات" Binding="{Binding PurchaseVatAmount, StringFormat=N2}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource SuccessBrush}" /></Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="صافي الضريبة" Binding="{Binding NetVat, StringFormat=N2}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock"><Setter Property="FontWeight" Value="Bold" /></Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
