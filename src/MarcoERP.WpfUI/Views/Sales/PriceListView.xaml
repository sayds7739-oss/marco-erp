<UserControl x:Class="MarcoERP.WpfUI.Views.Sales.PriceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:MarcoERP.WpfUI.Common"
             FlowDirection="RightToLeft"
             local:LoadedCommandBehavior.Command="{Binding LoadCommand}">

    <UserControl.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}" />
    </UserControl.InputBindings>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ══════ Row 0: Title + Toolbar ══════ -->
        <Border Grid.Row="0" Style="{StaticResource CardPanel}" Padding="12,8" Margin="0,0,0,4">
            <DockPanel>
                <TextBlock Text="قوائم الأسعار" Style="{StaticResource PageTitle}"
                           DockPanel.Dock="Right" Margin="0" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" HorizontalAlignment="Left">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource SuccessBrush}" Foreground="White"
                            Command="{Binding NewCommand}" ToolTip="إضافة قائمة أسعار جديدة" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" />
                            <TextBlock Text="قائمة جديدة" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource PrimaryBrush}" Foreground="White"
                            Command="{Binding SaveCommand}"
                            Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"
                            ToolTip="حفظ قائمة الأسعار" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" VerticalAlignment="Center" />
                            <TextBlock Text="حفظ" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding CancelCommand}"
                            Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"
                            ToolTip="إلغاء التعديل" Margin="4,0">
                        <TextBlock Text="إلغاء" />
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="{StaticResource DangerBrush}"
                            Command="{Binding DeleteCommand}" ToolTip="حذف قائمة الأسعار" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Delete" VerticalAlignment="Center" />
                            <TextBlock Text="حذف" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding LoadCommand}" ToolTip="تحديث البيانات" Margin="4,0">
                        <materialDesign:PackIcon Kind="Refresh" />
                    </Button>
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding PrintCommand}" ToolTip="طباعة قائمة الأسعار PDF" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Printer" VerticalAlignment="Center" />
                            <TextBlock Text="طباعة" Margin="6,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ══════ Row 1: Error Bar ══════ -->
        <Border Grid.Row="1" Background="{StaticResource ErrorBackgroundBrush}" CornerRadius="4" Padding="10,6" Margin="8,2"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AlertCircle" Foreground="{StaticResource ErrorForegroundBrush}" VerticalAlignment="Center" />
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorForegroundBrush}"
                           Margin="8,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- ══════ Row 2: Header — Price List Selector + Form Fields ══════ -->
        <Border Grid.Row="2" Style="{StaticResource CardPanel}" Padding="12,8" Margin="0,0,0,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Price List Selector -->
                <TextBlock Grid.Column="0" Text="قائمة الأسعار:" VerticalAlignment="Center"
                           FontSize="12" Foreground="{StaticResource SubtitleBrush}" Margin="0,0,8,0" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding AllPriceLists}"
                          SelectedItem="{Binding SelectedPriceList, Mode=TwoWay}"
                          DisplayMemberPath="NameAr" FontSize="13" Margin="0,0,12,0"
                          materialDesign:HintAssist.Hint="اختر قائمة أسعار" MinWidth="180" />

                <!-- Name Arabic -->
                <TextBlock Grid.Column="2" Text="الاسم:" VerticalAlignment="Center"
                           FontSize="12" Foreground="{StaticResource SubtitleBrush}" Margin="0,0,8,0" />
                <TextBox Grid.Column="3" Text="{Binding FormNameAr, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding IsEditing}" FontSize="13" Padding="6,4" Margin="0,0,12,0"
                         materialDesign:HintAssist.Hint="اسم القائمة بالعربية" />

                <!-- Valid From -->
                <TextBlock Grid.Column="4" Text="من:" VerticalAlignment="Center"
                           FontSize="12" Foreground="{StaticResource SubtitleBrush}" Margin="0,0,4,0" />
                <DatePicker Grid.Column="5" SelectedDate="{Binding FormValidFrom}"
                            IsEnabled="{Binding IsEditing}" FontSize="12" Margin="0,0,12,0" Width="120" />

                <!-- Valid To -->
                <TextBlock Grid.Column="6" Text="إلى:" VerticalAlignment="Center"
                           FontSize="12" Foreground="{StaticResource SubtitleBrush}" Margin="0,0,4,0" />
                <DatePicker Grid.Column="7" SelectedDate="{Binding FormValidTo}"
                            IsEnabled="{Binding IsEditing}" FontSize="12" Width="120" />
            </Grid>
        </Border>

        <!-- ══════ Row 3: Filter Bar + Bulk Actions ══════ -->
        <Border Grid.Row="3" Style="{StaticResource CardPanel}" Padding="10,6" Margin="0,0,0,4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Top row: Bulk actions + Search -->
                <DockPanel Grid.Row="0" Margin="0,0,0,4">
                    <!-- Bulk action buttons on the left -->
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" HorizontalAlignment="Left">
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding SelectAllCommand}"
                                ToolTip="تحديد كل الأصناف المعروضة" Margin="2,0" Padding="6,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="CheckboxMultipleMarked" VerticalAlignment="Center" Width="18" Height="18" />
                                <TextBlock Text="تحديد الكل" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="11" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding DeselectAllCommand}"
                                ToolTip="إلغاء تحديد كل الأصناف المعروضة" Margin="2,0" Padding="6,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="CheckboxMultipleBlankOutline" VerticalAlignment="Center" Width="18" Height="18" />
                                <TextBlock Text="إلغاء الكل" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="11" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding SelectVisibleCommand}"
                                ToolTip="تحديد الأصناف المعروضة حسب الفلتر الحالي" Margin="2,0" Padding="6,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="CheckboxMarkedCircle" VerticalAlignment="Center" Width="18" Height="18" />
                                <TextBlock Text="تحديد المعروض" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="11" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding CopyDefaultPricesCommand}"
                                ToolTip="نسخ سعر البيع الافتراضي لكل صنف محدد" Margin="2,0" Padding="6,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ContentCopy" VerticalAlignment="Center" Width="18" Height="18" />
                                <TextBlock Text="نسخ الأسعار" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="11" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Search on the right -->
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                             materialDesign:HintAssist.Hint="🔍 بحث بالاسم أو الكود..."
                             FontSize="12" Padding="6,4" MinWidth="200" HorizontalAlignment="Right"
                             DockPanel.Dock="Right" />
                </DockPanel>

                <!-- Bottom row: Filters -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Selection Mode -->
                    <TextBlock Text="عرض:" VerticalAlignment="Center" FontSize="11"
                               Foreground="{StaticResource SubtitleBrush}" Margin="0,0,4,0" />
                    <ComboBox SelectedIndex="{Binding FilterSelectionModeIndex}"
                              FontSize="11" MinWidth="110" Margin="0,0,10,0"
                              materialDesign:ComboBoxAssist.ShowSelectedItem="True">
                        <ComboBoxItem Content="الكل" />
                        <ComboBoxItem Content="المحددة فقط" />
                        <ComboBoxItem Content="غير المحددة" />
                    </ComboBox>

                    <!-- Code Range -->
                    <TextBlock Text="من كود:" VerticalAlignment="Center" FontSize="11"
                               Foreground="{StaticResource SubtitleBrush}" Margin="0,0,4,0" />
                    <TextBox Text="{Binding FilterCodeFrom, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                             FontSize="11" Padding="4,3" Width="80" Margin="0,0,8,0"
                             materialDesign:HintAssist.Hint="بداية" />
                    <TextBlock Text="إلى:" VerticalAlignment="Center" FontSize="11"
                               Foreground="{StaticResource SubtitleBrush}" Margin="0,0,4,0" />
                    <TextBox Text="{Binding FilterCodeTo, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                             FontSize="11" Padding="4,3" Width="80" Margin="0,0,10,0"
                             materialDesign:HintAssist.Hint="نهاية" />

                    <!-- Supplier Filter -->
                    <ComboBox ItemsSource="{Binding Suppliers}"
                              SelectedValue="{Binding FilterSupplierId, Mode=TwoWay}"
                              SelectedValuePath="Id" DisplayMemberPath="NameAr"
                              materialDesign:HintAssist.Hint="المورد"
                              FontSize="11" MinWidth="130" Margin="0,0,8,0"
                              IsEditable="False" materialDesign:ComboBoxAssist.ShowSelectedItem="True" />

                    <!-- Category Filter -->
                    <ComboBox ItemsSource="{Binding Categories}"
                              SelectedItem="{Binding FilterCategoryName, Mode=TwoWay}"
                              materialDesign:HintAssist.Hint="الفئة"
                              FontSize="11" MinWidth="110" Margin="0,0,8,0"
                              IsEditable="False" materialDesign:ComboBoxAssist.ShowSelectedItem="True" />

                    <!-- Clear Filters -->
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding ClearFiltersCommand}"
                            ToolTip="مسح جميع الفلاتر" Padding="4,2" Margin="0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FilterRemove" VerticalAlignment="Center" Width="16" Height="16" />
                            <TextBlock Text="مسح" Margin="3,0,0,0" VerticalAlignment="Center" FontSize="10" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ══════ Row 4: Excel-like DataGrid ══════ -->
        <Border Grid.Row="4" Style="{StaticResource CardPanel}" Margin="0,0,0,4">
            <DataGrid ItemsSource="{Binding FilteredItems}"
                      AutoGenerateColumns="False" IsReadOnly="False"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      SelectionMode="Single" GridLinesVisibility="All"
                      PreparingCellForEdit="PriceGrid_PreparingCellForEdit"
                      HeadersVisibility="Column" FontSize="11"
                      RowHeight="28" ColumnHeaderHeight="30" BorderThickness="0"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      VirtualizingPanel.IsContainerVirtualizable="True"
                      EnableRowVirtualization="True"
                      ScrollViewer.CanContentScroll="True"
                      materialDesign:DataGridAssist.CellPadding="6 2"
                      materialDesign:DataGridAssist.ColumnHeaderPadding="6 4">

                <DataGrid.Columns>
                    <!-- 1. Checkbox (IsSelected) -->
                    <DataGridTemplateColumn Header="✓" Width="45" CanUserResize="False" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- 2. Product Code -->
                    <DataGridTextColumn Header="الكود" Binding="{Binding ProductCode}" Width="90"
                                        IsReadOnly="True" />

                    <!-- 3. Product Name -->
                    <DataGridTextColumn Header="اسم الصنف" Binding="{Binding ProductName}" Width="2*"
                                        IsReadOnly="True" />

                    <!-- 4. Category -->
                    <DataGridTextColumn Header="الفئة" Binding="{Binding CategoryName}" Width="105"
                                        IsReadOnly="True" />

                    <!-- 5. Supplier -->
                    <DataGridTextColumn Header="المورد" Binding="{Binding SupplierName}" Width="95"
                                        IsReadOnly="True" />

                    <!-- 6. Default Sale Price (read-only) -->
                    <DataGridTextColumn Header="سعر افتراضي" Width="100" IsReadOnly="True">
                        <DataGridTextColumn.Binding>
                            <Binding Path="DefaultSalePrice" StringFormat="N2" />
                        </DataGridTextColumn.Binding>
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FlowDirection" Value="LeftToRight" />
                                <Setter Property="TextAlignment" Value="Right" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- 7. Major Unit (read-only) -->
                    <DataGridTextColumn Header="الوحدة الكلية" Binding="{Binding MajorUnitName}" Width="95"
                                        IsReadOnly="True" />

                    <!-- 8. Minor Unit (read-only) -->
                    <DataGridTextColumn Header="الوحدة الجزئية" Binding="{Binding MinorUnitName}" Width="95"
                                        IsReadOnly="True" />

                    <!-- 9. Major Unit Price (EDITABLE) -->
                    <DataGridTemplateColumn Header="سعر الكلية" Width="110" CanUserSort="True" SortMemberPath="MajorUnitPrice">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding MajorUnitPrice, StringFormat=N2}"
                                           FlowDirection="LeftToRight" TextAlignment="Right"
                                           VerticalAlignment="Center" Padding="4,0" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding MajorUnitPrice, UpdateSourceTrigger=LostFocus, StringFormat=N2}"
                                         FlowDirection="LeftToRight" TextAlignment="Right"
                                         VerticalAlignment="Center" Padding="4,2"
                                         BorderThickness="1" FontSize="12"
                                         GotKeyboardFocus="EditablePriceTextBox_GotKeyboardFocus"
                                         PreviewMouseLeftButtonDown="EditablePriceTextBox_PreviewMouseLeftButtonDown" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- 10. Minor Unit Price (EDITABLE) -->
                    <DataGridTemplateColumn Header="سعر الجزئية" Width="110" CanUserSort="True" SortMemberPath="MinorUnitPrice">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding MinorUnitPrice, StringFormat=N2}"
                                           FlowDirection="LeftToRight" TextAlignment="Right"
                                           VerticalAlignment="Center" Padding="4,0" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding MinorUnitPrice, UpdateSourceTrigger=LostFocus, StringFormat=N2}"
                                         FlowDirection="LeftToRight" TextAlignment="Right"
                                         VerticalAlignment="Center" Padding="4,2"
                                         BorderThickness="1" FontSize="12"
                                         GotKeyboardFocus="EditablePriceTextBox_GotKeyboardFocus"
                                         PreviewMouseLeftButtonDown="EditablePriceTextBox_PreviewMouseLeftButtonDown" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- 11. Major Activation Quantity (EDITABLE) -->
                    <DataGridTemplateColumn Header="عدد الجزء" Width="90" CanUserSort="True" SortMemberPath="PartCount">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PartCount, StringFormat=N0}"
                                           FlowDirection="LeftToRight" TextAlignment="Right"
                                           VerticalAlignment="Center" Padding="4,0" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding PartCount, UpdateSourceTrigger=LostFocus, StringFormat=N0}"
                                         FlowDirection="LeftToRight" TextAlignment="Right"
                                         VerticalAlignment="Center" Padding="4,2"
                                         BorderThickness="1" FontSize="12" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- ══════ Row 5: Status Bar ══════ -->
        <Border Grid.Row="5" Padding="10,6" Margin="8,0,8,0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <TextBlock FontSize="12" Foreground="{StaticResource SubtitleBrush}">
                        <Run Text="المحدد:" />
                        <Run Text="{Binding SelectedCount, Mode=OneWay}" FontWeight="SemiBold" />
                        <Run Text=" | الإجمالي:" />
                        <Run Text="{Binding TotalVisible, Mode=OneWay}" FontWeight="SemiBold" />
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SubtitleBrush}" FontSize="12" />
                    <ProgressBar IsIndeterminate="{Binding IsBusy}"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                                 Height="3" Width="100" Margin="16,0,0,0" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
