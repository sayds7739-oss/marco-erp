<Window x:Class="MarcoERP.WpfUI.Views.Shell.ShortcutConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="تخصيص الاختصارات"
        Width="480" Height="560"
        MinHeight="400" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        ResizeMode="CanResizeWithGrip"
        Style="{StaticResource MaterialDesignWindow}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="اختر حتى 6 اختصارات تظهر في لوحة التحكم"
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />

        <!-- Selection count -->
        <TextBlock Grid.Row="1" x:Name="SelectionCountText"
                   FontSize="12" Foreground="Gray" Margin="0,0,0,8"
                   Text="المحدد: 0 / 6" />

        <!-- Available screens list -->
        <ListBox Grid.Row="2" x:Name="ScreensListBox"
                 SelectionMode="Multiple"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Title}"
                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                              Checked="CheckBox_Changed"
                              Unchecked="CheckBox_Changed"
                              Margin="4,2" FontSize="13" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,12,0,0">
            <Button Content="حفظ" Click="OkButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0" MinWidth="90" />
            <Button Content="إلغاء" Click="CancelButton_Click" IsCancel="True"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    MinWidth="90" />
        </StackPanel>
    </Grid>
</Window>
